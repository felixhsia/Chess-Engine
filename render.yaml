services:
  - type: web
    name: xiangqi-advisor-api
    runtime: python
    buildCommand: bash build.sh
    startCommand: gunicorn app:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ENGINE_PATH
        value: ./fairy-stockfish
